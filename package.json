{
  "name": "ticker-tactix-video-smusher",
  "version": "1.0.0",
  "description": "Hosted browser-based video merger for local intro/outro assets and Blob source uploads.",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/prune-ffprobe.js && node scripts/prepare-binaries.js && esbuild src/client.js --bundle --format=iife --platform=browser --target=es2020 --outfile=public/renderer.js",
    "start": "node server.js",
    "dev": "node server.js",
    "verify:assets": "node scripts/verify-assets.js"
  },
  "keywords": [
    "video",
    "ffmpeg",
    "assets",
    "vercel"
  ],
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "@vercel/blob": "^2.4.0",
    "express": "^5.2.1",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
